namespace OpenPixel2D.Content;

/// <summary>
/// Normalized runtime pixel formats emitted by the content system.
/// Images are decoded into a backend-neutral layout suitable for later GPU upload.
/// </summary>
public enum RuntimeImagePixelFormat
{
    Rgba32 = 0
}
